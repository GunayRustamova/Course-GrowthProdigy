@model Student
@{
    var today = DateTime.Today;
    var age = today.Year - Model.StudentDetail.DateOfBirth.Year;
}
<section id="about" class="about">
    <div class="container">
        <div class="section-title">
            <h1 style="margin: 0 0 10px 0; font-size:64px; font-weight:700; line-height:56px; color:#173b6c">@Model.Name @Model.Surname</h1>
            <h2 style="font-size:32px; font-weight:bold; margin-bottom:20px; padding-bottom:20px; position:relative; color:#173b6c; font-family:'Raleway', sans-serif; line-height:1.2; box-sizing:border-box;">About</h2>
        </div>
        <div class="row">

            <div class="col-lg-8 pt-4 pt-lg-0 content aos-init aos-animate" data-aos="fade-left">
                <h3 style="font-weight:700; font-size:26px; color:#173b6c;">@Model.Course.Name @Model.Group.GroupNumber</h3>
                <div class="row">
                    <div class="col-lg-6">
                        <ul>
                            <li>
                                <i class="bi bi-chevron-right"></i>
                                <strong>Father Name:</strong>
                                <span>@Model.StudentDetail.FatherName</span>
                            </li>
                            <li>
                                <i class="bi bi-chevron-right"></i>
                                <strong>Birthday:</strong>
                                <span>@Model.StudentDetail.DateOfBirth.ToString("dd MMMM yyyy")</span>
                            </li>

                            <li>
                                <i class="bi bi-chevron-right"></i>
                                <strong>Phone:</strong>
                                <span>@Model.StudentDetail.PhoneNumber</span>
                            </li>
                        </ul>
                    </div>
                    <div class="col-lg-6">
                        <ul>
                            <li>
                                <i class="bi bi-chevron-right"></i>
                                <strong>Gender:</strong>
                                <span>
                                    @if (Model.StudentDetail.Gender)
                                    {
                                        <span>Female</span>
                                    }
                                    else
                                    {
                                        <span>Male</span>
                                    }
                                </span>
                            </li>
                            <li>
                                <i class="bi bi-chevron-right"></i>
                                <strong>Age:</strong>
                                <span>@age</span>
                            </li>
                            <li>
                                <i class="bi bi-chevron-right"></i>
                                <strong>Address:</strong>
                                <span>@Model.StudentDetail.Address</span>
                            </li>

                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>